# Keeps the SHA-pinned GitHub Actions current: opens a weekly PR bumping the
# pinned commit (and its version comment). Nothing merges automatically.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      github-actions:
        patterns: ['*']

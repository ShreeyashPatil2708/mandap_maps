// Outbound links used by the footer and the privacy page. Kept in one module so
// the contact address is not retyped at each call site.

export const CONTACT_EMAIL = 'connect@mandapmaps.in';

export const CONTACT_MAILTO = `mailto:${CONTACT_EMAIL}`;

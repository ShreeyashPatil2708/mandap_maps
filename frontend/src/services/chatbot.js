/* eslint-disable no-unused-vars -- params are consumed once the real API is wired in */
export async function callChatbotAPI(message, ganpatiContext) {
  // CHATBOT_INTEGRATION_POINT
  // POST /api/chat - body: { message, ganpati_id }
  // Response: { reply: string }
  return "The chatbot is coming soon!";
}
